#include <iostream>
#include <Bike.h>

#include <string>

Bike() {
    brand = "";// default constructor - makes new bike with code number 0 and empty string "" for brand
    code = 0;
}

Bike(std::string brand, int code) {

    std::string getBrand(); //Getter of the brand

    int getCode(code = n) { //Getter of the code
        std::cout << "enter the code" << std::endl;
        std::cin >> 'n'
    }

}

~Bike() //destructor

